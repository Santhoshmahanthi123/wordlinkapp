const express = require("express");
const router  = express.Router();
const wordsController = require('../controllers/words');
// router.get('/',wordsController.words_get_all);
module.exports = router;